# AGENTS.md

给 AI 编程助手与新成员的规则。以代码和 `package.json` 为准，文档只写仓库实际提供的东西。

## 先读

- 开发流程：`docs/DEVELOPMENT.md`
- 改界面前：`DESIGN.md`；界面改动兼顾宽屏 / 窄屏与浅色 / 深色
- 领域术语：`CONTEXT.md`；已定决策：`docs/adr/`

## 命令

在仓库根目录执行：`pnpm dev`、`pnpm test`、`pnpm test:e2e`（首次需 `pnpm exec playwright install chromium`）、`pnpm lint`、`pnpm typecheck`、`pnpm format`。全部检查：`pnpm check`。

## 模块边界

- `src/services/`：界面唯一的数据入口，可按领域分文件；组件不绕过它直接请求
- 同一份数据只保留一个状态源和一条写入路径，不用延时或超时掩盖时序问题
- `src/components/ui/`：通用基础组件，界面优先复用，不依赖业务；`src/features/`：业务界面；`src/app/`：路由、布局与主题
- `src/types/`：界面实际用到的类型，不为还没做的功能预留
- 颜色与字号只用 `src/styles/tokens.css` 定义的语义 token

## 底线

- 不显示假成功：未实现的功能写明“尚未实现”；请求失败显示失败与重试，不拿演示数据顶替，不把中断的回答标成完成
- 演示内容标注“演示模式 / 非真实检索结果”；不编造政策条文、研究结论或模型效果
- 密钥、模型与检索调用只在后端；前端只调用本项目后端接口，`VITE_` 环境变量会打包公开，不放密钥；模型返回的内容按不可信文本渲染
- 代码、日志、示例和提交中不写入凭据与真实用户数据；浏览器本地存储只放主题、侧栏等非敏感界面偏好
- 提交、推送、合并、部署和破坏性 Git 操作（reset --hard、强制推送、删分支等）需用户明确同意；不改远端设置（公开性、分支保护、权限）

## 做事方式

- 手续随任务大小伸缩：文案、样式、单处缺陷直接改；新功能先明确目标、不做项和验收方式；接口格式等从代码推断不出、又会改变方案的信息先问，不自行编造
- 用户只报告问题、没要求改代码时，先调查原因再汇报，分清已确认的原因与待验证的猜测
- 修问题先找根因；发现是设计缺陷时先与用户对齐，不层层加兜底分支
- 不擅自扩大范围或更换技术栈；影响全局的依赖（状态管理库、整套 UI 库等）先问并写 ADR，局部小库可直接引入，在 PR 中说明用途
- 不覆盖别人未提交的改动
- 改了行为就补测试：逻辑放 `src/**/*.test.ts(x)`，交互流程放 `tests/e2e/`；不写空测试，不靠跳过测试或关闭规则通过
- 交付前运行 `pnpm check` 并如实报告：没运行的写明没运行，分清原有问题与本次引入的问题
- 功能增删、术语、设计规则或决策变化时，同步 `README.md`、`CONTEXT.md`、`DESIGN.md` 或 `docs/adr/` 中对应的内容，删除功能时一并清理引用
