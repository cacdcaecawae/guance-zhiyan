# DESIGN.md

本项目的视觉与交互规范。基调：安静、紧凑、清晰、适合长时间阅读的研究工具；不是营销页，也不是数据驾驶舱。

风格名为“公文”：白纸黑字、衬线正文，藏青负责一切可交互元素，朱红只做装饰（“问”字印章、编号、当前页签、标志），与中国社会科学院大学的中国红相呼应。中性色接近纯中性、只带一点暖意，不用明显泛黄的纸色；深色正文不用纯白以免中文发粗；轨迹分类色取靛蓝、赭石、橄榄等矿物色。对话是多轮往下排的问答，只有文件预览画成“一页纸”，文件与对话一眼可分。

参考来源：规则裁剪自 [ZCode DESIGN.md](https://github.com/zai-org/ZCode/blob/main/DESIGN.md)（语义 tokens、`text-ui-*` 字号体系、圆角嵌套递减、克制的层次与阴影）。本文件是本地规范，不自动跟随上游变化。

## 颜色

所有颜色只在 `src/styles/tokens.css` 定义，浅色在 `:root`，深色在 `.dark`。组件只用语义类名，不写硬编码颜色（hex、`white` / `black` 等非语义色及其透明度变体，如 `text-white/60`）；语义 token 可加透明度，仅用于弱边框与悬停（如 `border-brand/40`、`hover:bg-primary/90`）；遮罩用 `bg-overlay`。

| 类别     | Token（Tailwind 类前缀 `bg-` / `text-` / `border-`）                                                                         |
| -------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 结构面   | `background` 页面、`sidebar` 侧栏、`surface` / `surface-hover` 内容面、`card` / `card-border` 卡片                           |
| 边框     | `border` 分隔线与控件边框、`border-hover` 悬停边框                                                                           |
| 浮层     | `popover` / `popover-border`（抽屉、菜单、对话框）、`overlay` 遮罩                                                           |
| 输入     | `input` / `input-focused`、`input-border` / `input-border-hover` / `input-border-focused`                                    |
| 文本     | `foreground` 正文、`foreground-subtle` 次要、`foreground-subtlest` 弱提示                                                    |
| 状态面   | `hover` 悬停、`selected` 选中、`accent` 弱强调面（激活引用）、`tag` 徽标与禁用的主按钮、`find-highlight` 片段高亮            |
| 强调     | `brand` 藏青：链接、焦点环、聚焦边框；`primary` / `primary-foreground` 藏青主按钮；`seal` / `seal-foreground` 朱红，只做装饰 |
| 语义状态 | `destructive` 错误，只用于真实状态，不做装饰；需要成功、警告等状态色时再补，并复核对比度                                     |

规则：

- 页面根 `bg-background text-foreground`；侧栏 `bg-sidebar`；主内容区只在 `<main>` 上铺一次 `bg-surface`，顶栏与页面不再各自铺底；卡片 `bg-card`；浮层 `bg-popover`
- 朱红（`seal`）只用于“问”字印章、有序列表编号、当前页签下划线、当前会话标记、文件格式标签和文档标题下的细线，不表达任何状态；错误用 `destructive` 并总是配警示图标，不单靠红色区分
- 品牌色不做大面积填充；不使用渐变背景、玻璃效果，标志（朱漆鎏金）是唯一例外
- 用文字层级表达信息密度，再考虑加边框或颜色
- 文字颜色对其所在背景的对比度不低于 4.5:1（WCAG AA），`foreground-subtlest` 也不例外；修改 `tokens.css` 或把文字放到新背景上时复核

## 字体与字号

- 界面控件用系统无衬线栈（`font-sans`）；阅读内容用 `font-serif`：问题、最终回答正文、文件预览、顶栏会话标题、欢迎标题与校名署名。衬线栈不列宋体（SimSun 无粗体），缺衬线字体时回落无衬线；执行过程、表格与界面说明仍用无衬线；`font-mono` 只用于路径、耗时、工具参数等技术值
- 不引用外部字体 CDN
- 界面文字只用 `text-ui-*`，不用 Tailwind 内置 `text-sm` / `text-base` 或任意像素值。基准 `--ui-font-size: 14px`

| Token             | 大小 | 用途                                   |
| ----------------- | ---- | -------------------------------------- |
| `text-ui-display` | 26px | 仅空会话的欢迎标题                     |
| `text-ui-xl`      | 18px | 页面级标题、回答内一二级标题           |
| `text-ui-lg`      | 16px | 问题、顶栏会话标题、回答内三级标题     |
| `text-ui-prose`   | 15px | 问题与最终回答正文，行高 1.75          |
| `text-ui-base`    | 14px | 界面正文、按钮、输入、面板标题（默认） |
| `text-ui-caption` | 13px | 需要比正文低一级的紧凑说明             |
| `text-ui-sm`      | 12px | 辅助说明、分组标题、提示               |
| `text-ui-xs`      | 10px | 仅弱徽标与引用角标，不承担正文阅读     |

标题层级优先用字重（`font-medium` / `font-semibold`）区分，不靠放大字号。新增或删除 `--text-ui-*` 时同步 `src/lib/cn.ts` 的 tailwind-merge 字号组，否则字号会被文字颜色类覆盖。

回答正文排版（`globals.css` 的 `.answer-markdown`，间距以 em 随字号缩放）：段落间距 0.8em、不首行缩进；开启 `text-autospace` 中西文间距与 `text-wrap: pretty`；强调 `em` 用着重号而非伪斜体；有序列表编号为朱红；引文左侧细线；表格为三线表（顶底粗线、表头细线、无竖线，数字等宽）；链接藏青、下划线变淡，悬停加深。文件预览里文档的首个一级标题居中、下压一道朱红细线。

## 间距与尺寸

- 基础单位 4px，常用 8 / 12 / 16 / 24px（`gap-2` `p-3` `p-4` `gap-6`）
- 控件高度 `h-7` / `h-8`；图标 `size-4` 为基准；图标按钮 `size-7` / `size-8` 正方形
- 对话与输入区 `w-full max-w-4xl` 居中；轨迹铺满工作区
- 可拖动的尺寸：侧栏宽度（208–400px）、文件预览宽度（320–760px）、输入框最小高度（48–480px），都用 `ResizeHandle`（指针拖动或方向键），作为界面偏好写 localStorage
- 弹性布局中需要截断时加 `min-w-0`，嵌套滚动时加 `min-h-0`
- 长链接、长编号由 `body` 上的 `wrap-break-word` 自动折行，块级文本不必各自处理；flex 行内可能出现长串的项需加 `min-w-0`；需要单行时用 `truncate`

## 圆角

按可见圆角容器的嵌套层级递减，不按重要性：

- 主内容区的第一层圆角容器用 `rounded-xl`（输入区外壳、占位卡片）；侧面板（资料面板等）内的内容块与菜单弹层用 `rounded-lg`
- 嵌套依次 `rounded-lg` → `rounded-md` → `rounded-sm`
- 基础控件默认 `rounded-lg`；放在圆角容器内时用 `rounded-md`（如主题切换分段、输入区内的发送按钮、回答卡片内的重试按钮）
- 徽标与引用角标 `rounded-sm`
- 不用 `rounded-2xl` 或裸 `rounded`；`rounded-full` 只用于悬浮的圆形“回到底部”按钮；边缘贴合的抽屉不加圆角；文件预览的“纸”用 `rounded-sm`

## 标志

`src/app/logo.tsx`，图形资产，颜色固定、不随主题变化：

- `Nameplate` 铭牌：朱漆底、鎏金双线框，左上与右下「」角饰（引文可溯源），鎏金衬线“管策智研”带暗红压影。侧栏顶部使用，下方以衬线小字署名“中国社会科学院大学”。
- `Seal` 方印：单字“策”，用于网站图标、欢迎状态和连接页；小于 40px 时去掉内框。
- 鎏光：一道光带扫过铭牌，悬停时扫一次（`sheen="once"` 为挂载后一次），`prefers-reduced-motion` 时不播放；这是全站唯一的装饰动效。
- 字形取自 Noto Serif SC（SIL OFL 1.1）wght 900 并转成路径（`logo-glyphs.ts`），不依赖用户字体；`public/favicon.svg` 是同一方印。
- 不使用或改动学校官方校徽；需要时按学校视觉识别规范原样放置并经学校同意。

## 层次与阴影

- 主要靠背景对比和边框分层；页面与侧栏无阴影
- 浮层（抽屉、对话框、菜单）与悬浮的“回到底部”按钮用 `shadow-md`；输入区外壳与文件预览的“纸”用 `shadow-sm`；不做厚重阴影
- 动效保持短促克制：状态变化用 `transition-colors`，加载指示可用 `animate-spin`；不做装饰性的入场、位移、缩放动效

## 组件

基础组件在 `src/components/ui/`，均为 shadcn/ui 风格、绑定本项目 tokens：

- `Button`：`default` 主按钮（每个面板最多一个）、`outline`、`ghost`；尺寸 `default` / `sm` / `icon` / `icon-sm`
- `Textarea`：`bg-input` + `border-input-border`，悬停与聚焦只改边框色，不发光
- `Sheet`：窄屏抽屉，带 `sr-only` 标题与“关闭”按钮
- `Badge`：通用弱徽标
- `Select`：基于 Radix Select 的下拉，弹层 `bg-popover rounded-lg shadow-md`，选项 `rounded-md`，选中项用尾部对勾，不填色
- `ResizeHandle`：可拖动分隔条（`role="separator"`），支持指针与方向键、Home / End

交互元素必须有悬停与键盘焦点（`focus-visible:ring-2 ring-ring`）状态，可禁用的控件要有禁用样式；切换、当前项、展开分别用 `aria-pressed`、`aria-current`、`aria-expanded` 表达，普通按钮和链接不加；图标按钮必须有 `aria-label`。

## 布局

```
左侧导航与会话列表 | 中央研究工作区（问答、执行过程与会话文件） | 右侧文件预览（打开时）
```

- ≥768px：侧栏常驻，可通过顶部栏按钮折叠（偏好写 localStorage）；<768px：侧栏为左侧抽屉
- 右侧文件预览：点击会话文件名打开，宽屏（≥1024px）为可拖宽的右栏，窄屏为右侧抽屉；灰色桌面上一页纸，Markdown 按正文排版、CSV 为三线表、文本等宽显示；Word / Excel 等暂不支持预览时写明并提供下载。资料面板（文献片段）随真实文献库引用后续实现，当前不显示虚构片段
- 顶部栏 `h-12`：侧栏开关、会话标题（截断并带 `title`）；工作区的“对话 / 轨迹”页签放在右侧，当前页签文字加深并以朱红下划线压住顶栏底线；会话标题用衬线粗体
- 中央区：消息列表可滚动，输入区固定在底部；空会话时欢迎状态、示例问题与输入区一起垂直居中
- 主题：浅色 / 深色 / 跟随系统，`.dark` 类挂在 `html` 上，`index.html` 内联脚本避免首屏闪烁；`color-scheme` 随主题切换，原生滚动条与控件跟随所选主题

## 回答与执行状态

- 空结果、失败与重试有明确文案；未实现功能显示“尚未实现”，不显示假成功
- 应用无演示模式；测试模型只存在于自动化测试中
- 正文用安全 Markdown 渲染，禁止原始 HTML、危险链接及外部图片自动加载；表格和代码块局部滚动
- 工作区提供“对话 / 轨迹”页签，支持键盘左右切换。
- 对话按公文风格多轮排版：每一问以朱红“问”字印章起头、衬线粗体；回答缩进到印章之后，正文衬线；轮次之间细线分隔；不用左右气泡。
- 执行过程按 DSH 的汇总行与紧凑事件行排列，整体像页边批注：左侧细线、`text-ui-caption` 次要色；汇总按工具种类计数（如“联网搜索 2 次 · 生成文件 1 次”），另列失败次数，没有工具时写“执行过程”。工具状态放在图标位（执行中转圈并显示文字、失败为红色警示图标），状态文字对读屏保留。思考与工具详情默认折叠，过程整体可收起；最终回答保持独立可见。
- 回答下方操作行：复制最终回答原文（剪贴板不可用时显示“复制失败”，不假装成功）、失败或停止后的“重新提问”。离开底部阅读时出现“回到底部”。
- 轨迹按真实轮次展示系统、用户、上下文、助手和工具事件，展开查看内容；轮次标题吸顶。“时长”切换记录时间与等宽事件顺序，“轮次”切换全部轮次展开，“调用”切换工具行显示。时间分布带 25/50/75% 刻度并显示总记录耗时；状态与耗时各占固定列，耗时用等宽数字。未发生的事件不展示，不叠加并行工具耗时。
- 轨迹分类颜色只用 trace-model / trace-tool / trace-context 语义 token；窄屏摘要省略，展开内容换行或局部滚动。中止、失败和连接错误在两个视图均可辨认。
- 生成中提供停止按钮；完成、失败、停止分别展示，不将网络断开误判为生成完成
- 输入框底行右侧是模型下拉（`ModelPicker`，参照 Claude，紧挨发送按钮）：按平台分组列出全部 DeepSeek 模型，触发器显示“模型 · 平台”（两平台有同名模型），生成中禁用；缺少密钥的平台在分组标题和触发器中标出；选择随成功发送保存至会话。最右是发送按钮（藏青，禁用时用 `bg-tag`，不靠半透明），生成中换成带文字的“停止”。输入框顶边的把手可拖动调整高度
- 界面不放防御性提示语（如“所选平台会接收历史”“需核对来源”）；未实现的页面本身仍写明“尚未实现”
- 请求失败保留草稿；失败或停止后“重新提问”追加一次新的提问，保留原始记录
- 文件生成成功后才出现在“会话文件”中：两列文件卡片（窄屏一列），左侧朱红格式印块、文件名与大小，整卡点击在右侧预览（打开时藏青描边并标“预览中”），右侧下载图标按钮；文献库和成果归档未实现时明确说明
