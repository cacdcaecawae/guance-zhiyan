diff --git a/lib/index.js b/lib/index.js
index c220a327b7b9d2d73e4e407179f2208b074be0eb..3b0075519608dc9f3b6629c679a97db7e5dd8af6 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1517,8 +1517,8 @@ function providerError(raw, status, headers) {
 * @param activity - pulse the idle watchdog for events and heartbeat comments.
 * @returns JSON events, including message_stop; the translator owns completion.
 */
-async function* parseSse(body, activity) {
-	const events = body.pipeThrough(new TextDecoderStream()).pipeThrough(new EventSourceParserStream({ onComment: activity }));
+async function* parseSse(body, activity, signal) {
+	const events = body.pipeThrough(new TextDecoderStream(), { signal }).pipeThrough(new EventSourceParserStream({ onComment: activity }), { signal });
 	for await (const frame of events) {
 		activity();
 		let raw;
@@ -1957,7 +1957,7 @@ var DeepSeekAdapter = class extends LlmAdapter {
 			}
 			await extensions.accept();
 			if (response.body === null) throw new LlmError("DeepSeek Messages returned no response body", "EMPTY_RESPONSE");
-			yield* translate(parseSse(response.body, activity), options.model);
+			yield* translate(parseSse(response.body, activity, signal), options.model);
 			return;
 		}
 	}
