# 管策智研

面向人文社科研究者的政策文本研究工作台：研究者围绕政策文本提出问题，得到可追溯到原文片段的回答。

## 术语

**会话**：
研究者与 AI 围绕研究问题依次问答的一次记录，由按时间排列的问题与回答组成。界面上的“新建研究”即新建一个会话。
_Avoid_：聊天

**问题**：
研究者在会话中提出的一条提问。

**回答**：
针对一条问题给出的答复，可包含格式文本、模型提供的思考内容与工具执行过程。

**引用**：
回答正文中指向可核对来源的标注。当前联网回答可附网页链接；文献库原文片段引用尚未实现。

**文献**：
一份政策文本或研究资料。

**片段**：
文献中可按章节与编号定位的一段原文。

**资料面板**：
展示所引片段原文及其文献信息的规划中界面区域，随 RAG 实现。

**工具**：
助手可调用的后端能力，例如联网搜索、网页读取和文件生成；配置沙箱后还包含隔离环境中的 Bash 与文件读写。

**执行过程**：
由真实会话事件派生的工具调用、结果与失败状态；与模型返回的思考文本分开展示。

**会话文件**：
当前会话通过工具生成或从工作区导出、归属于该研究者的可下载产物。内置生成器支持 Markdown、Word、Excel 与 CSV；沙箱产物可为其他格式，Markdown、CSV 与纯文本可在右侧预览，其他格式下载查看。

**会话沙箱**：
当前会话独立的命令与文件执行环境，首次使用时创建，停止或空闲时回收。其 `/workspace` 工作区文件持久保存，执行进程与内存不持久保存。

**轨迹**：
会话的事件视图，按轮次展示实际系统输入、用户消息、上下文、模型调用与工具结果，支持检查记录耗时和展开内容。
